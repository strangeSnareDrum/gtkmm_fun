Hi.

# Prerequisites
* gtkmm-3.0, install on Ubuntu:
apt-get install libgtkmm-3.0-dev

* cmake, download from 
https://cmake.org/download/

# Build and run
```bash
$ mkdir build
$ cd build
$ cmake .. && cmake --build .
$ ./gtkmmfun
```